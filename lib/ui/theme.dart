// Mengimpor package material dari Flutter untuk menggunakan tipe Color
import 'package:flutter/material.dart';

// Mendefinisikan kelas RiveAppTheme untuk menyimpan konstanta tema aplikasi
class RiveAppTheme {
  // Warna aksen utama aplikasi, menggunakan format heksadesimal (biru cerah)
  static const Color accentColor = Color(0xFF5E9EFF);
  // Warna untuk bayangan, menggunakan warna abu-abu gelap
  static const Color shadow = Color(0xFF4A5367);
  // Warna latar belakang utama, menggunakan warna biru sangat terang
  static const Color background = Color(0xFFF2F6FF);
  // Warna latar belakang sekunder, menggunakan warna biru tua
  static const Color background2 = Color(0xFF17203A);
  
  // static const Color shadowDark = Color(0xFF000000);
  // static const Color backgroundDark = Color(0xFF25254B);
}